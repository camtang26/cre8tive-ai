# GSAP Excellence Engine Module Configuration
# Generated by BMAD Builder
# Version: 6.0.0-alpha.0
# Date: 2025-10-11

module_name: "GSAP Excellence Engine"
module_code: "gsap-excellence"
author: "Cameron"
description: "Fight AI mediocrity with premium GSAP animations through specialized agents and multi-source research workflows"
category: "Technical - Animation & Motion Design"
complexity: "complex"
version: "1.0.0-alpha"

# Module paths
module_root: "{project-root}/bmad/gsap-excellence"
installer_path: "{project-root}/bmad/gsap-excellence"

# Component counts (Phase 1 MVP)
agents:
  count: 5
  mvp_count: 3
  list:
    - gsap-director      # Lead agent, orchestration, creative ideation
    - gsap-cinematographer  # Research specialist with MCP integration
    - gsap-vfx           # Implementation specialist
    - gsap-editor        # Debugging and refinement (Phase 2)
    - gsap-tech-director # Performance and testing (Phase 2)

workflows:
  count: 10
  mvp_count: 4
  list:
    - creative-ideation        # Signature workflow
    - implement-from-pattern   # Quick implementation
    - research-gsap-pattern    # Deep research
    - setup-gsap-project       # Project initialization
    - animation-production     # Full pipeline (Phase 2)
    - debug-animation          # Fix animations (Phase 2)
    - optimize-performance     # Performance profiling (Phase 2)
    - refine-timing            # Polish timing (Phase 2)
    - add-scroll-effects       # ScrollTrigger (Phase 3)
    - visual-regression-test   # Cross-device testing (Phase 3)

tasks:
  count: 0
  list: []

# Film Studio Theme
personality_theme: "Film/Animation Studio Crew"
mission: "Fight AI mediocrity, achieve animation excellence"
philosophy: "Optimize, Don't Satisfice"

# MCP Server Integrations (REQUIRED)
mcp_servers:
  archon:
    required: true
    purpose: "GSAP documentation and code examples via RAG"
    tools: ["rag_search_knowledge_base", "rag_search_code_examples", "rag_get_available_sources"]

  context7:
    required: true
    purpose: "Latest GSAP API documentation"
    tools: ["resolve-library-id", "get-library-docs"]

  perplexity:
    required: true
    purpose: "Premium animation examples and trends"
    tools: ["perplexity_ask", "perplexity_reason", "perplexity_research"]

  chrome_devtools:
    required: true
    purpose: "Visual validation and performance testing"
    tools: ["take_screenshot", "performance_start_trace", "emulate_cpu", "emulate_network"]

# Module-specific settings
settings:
  target_fps: 60
  performance_budget_paint_time: "16ms"
  quality_standard: "premium"
  accessibility: true
  browser_support: ["Chrome", "Firefox", "Safari"]

# Output configuration
output_folder: "{project-root}/docs/gsap-excellence"
data_folder: "{module_root}/data"
pattern_library: "{module_root}/patterns"
templates_folder: "{module_root}/templates"

# User configuration
user_name: "Cameron"
communication_language: "English"
